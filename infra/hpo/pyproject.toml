[project]
name = "rtrrl-hpo-control"
version = "0.1.0"
description = "Jump-host HPO advisor/scheduler for RTRRL experiments"
requires-python = ">=3.10"
dependencies = [
    "aim==3.28.0",
    "optuna>=4.0,<5",
    "pandas>=2.3.3",
    "pyyaml>=6.0,<7",
]

[tool.uv]
package = false

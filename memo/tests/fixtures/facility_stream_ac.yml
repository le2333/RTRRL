protocol_version: "1"
environment:
  name: memory_chain
  options:
    length: 4
    max_episode_steps: 4
logging:
  aim_every_env_steps: 4
  rerun_every_episodes: 1
parameters:
  agent_type: rtu_rtrl
  num_envs: 1
  num_epochs: 1
  eval_every: 1
  eval_steps: 4
training_budget:
  env_steps: 4
